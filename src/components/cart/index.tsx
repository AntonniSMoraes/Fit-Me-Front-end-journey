import styled from "styled-components"

export const Cart = () => {
    return(
        <Container>
            <h3>Cart</h3>
        </Container>
    );
}

const Container = styled.div`
    display: flex;
    flex-direction: column;
    width: 365px;
`